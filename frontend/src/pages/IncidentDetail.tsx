import {useEffect,useState} from 'react';
import {ArrowLeft,Clock,Network,Server,Shield,Terminal} from 'lucide-react';
import SeverityBadge from '../components/SeverityBadge';import RiskScore from '../components/RiskScore';import type {Incident} from '../types/incident';import {getIncident} from '../services/api';
export default function IncidentDetail({id,onBack}:{id:string;onBack:()=>void}){const [i,setI]=useState<Incident|null>(null);const [error,setError]=useState('');useEffect(()=>{getIncident(id).then(setI).catch(e=>setError(e.message))},[id]);if(error)return <div className="error page-error">{error}</div>;if(!i)return <div className="loading">Loading incident…</div>;return <main className="detail"><button className="back" onClick={onBack}><ArrowLeft size={18}/> Back to analysis</button><div className="detail-head"><div><div className="detail-tags"><SeverityBadge severity={i.severity}/><span className="mitre">{i.mitre_id} · {i.mitre_name}</span></div><h1>{i.attack_type}</h1><p>Suspicious activity grouped from {i.event_count} matching log events.</p></div><RiskScore score={i.risk_score}/></div><section className="facts"><div><Network/><span>Source IP</span><strong>{i.source_ip}</strong></div><div><Server/><span>Affected service</span><strong>{i.attack_type.includes('SSH')||i.attack_type.includes('Credential')?'SSH':'HTTP'}</strong></div><div><Clock/><span>First seen</span><strong>{i.first_seen?new Date(i.first_seen).toLocaleString():'Unknown'}</strong></div><div><Clock/><span>Last seen</span><strong>{i.last_seen?new Date(i.last_seen).toLocaleString():'Unknown'}</strong></div></section><section className="panel"><div className="panel-title"><h2><Terminal size={20}/> Evidence</h2><span>{i.evidence?.length||0} samples</span></div><div className="evidence">{i.evidence?.map((e,n)=><pre key={n}>{e.raw_log}</pre>)}</div></section><section className="panel remediation"><div className="panel-title"><h2><Shield size={20}/> Recommended remediation</h2></div><p>{i.recommendation}</p></section></main>}
